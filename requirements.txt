# --- Core Python Dependencies ---
# Generally required for all scripts (main.py, predict.py, app.py)
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0         # For progress bars in the custom Trainer

# --- PyTorch Deep Learning Dependencies ---
# Required for all DL models (LSTM, CNN, Attention, BERT, SBERT)
torch>=2.0.0
torchmetrics>=1.0.0  # For accurate metric calculation

# --- Text Preprocessing Dependencies ---
# Required for text cleaning and EDA
nltk>=3.8.1          # For text preprocessing and tokenization
wordcloud>=1.8.2    # For generating word clouds in EDA



# --- Hugging Face/Transformer Dependencies ---
# Required for BERT, SBERT, and Tokenization/DataModule
transformers>=4.30.0
datasets>=2.14.0     # For loading the dataset
sentence-transformers>=2.2.0 # Required explicitly for SBERT model

# --- Classical Machine Learning Dependencies ---
# Required for LogReg/SVM models and feature engineering
scikit-learn>=1.3.0
joblib>=1.3.0        # For saving and loading the ML pipeline

# --- Visualization and EDA Dependencies ---
# Required for the SentimentEDA class
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Streamlit Application Dependencies ---
# Required ONLY if you run the streamlit app (app.py)
streamlit>=1.28.0


    

-e .